# Kotlin Spring Application

This is a Kotlin application built with the Spring framework.

## Prerequisites

- JDK 11 or higher
- Gradle

## Getting Started

### Running the Application

To run the application, execute the following command:

```shell
./gradlew clean build
./gradlew bootRun
```
